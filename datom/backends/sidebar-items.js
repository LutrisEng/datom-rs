window.SIDEBAR_ITEMS = {"struct":[["RedBlackTreeSetStorage","A storage backend backed by a [RedBlackTreeSet]"],["SledStorage","A storage backend backed by a [sled] database"],["TieredStorage","A storage backend backed by two other storage backends Inserts are sent to both backends. Reads come from both backends, and any items which are read from B but not found in A will persisted to A in a future update."]]};